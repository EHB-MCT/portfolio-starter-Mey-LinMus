// const { v4: uuidv4 } = require("uuid");

exports.seed = async function (knex) {
  // await knex("users").del(); 

  // await knex("users").insert([
  //   { uuid: uuidv4(), name: "Phil Dunphy", birthday: "1967-08-22", age: 56 },
  //   { uuid: uuidv4(), name: "Claire Dunphy", birthday: "1970-03-03", age: 53 },

  // ]);
};
