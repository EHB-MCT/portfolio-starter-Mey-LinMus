exports.seed = async function (knex) {
  // await knex("comments").del();
  // // await knex("comments").insert([
  // //   { text: "So they've asked me to .. Phil in", user_id: 1 },
  // //   { text: "Happy birthday", user_id: 2 },
  // // ]);
};